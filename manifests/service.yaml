apiVersion: v1
kind: Service
metadata:
  name: will-playground-app-service
  namespace: dev
spec:
  selector:
    app: wil-playground
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8888
  type: ClusterIP
