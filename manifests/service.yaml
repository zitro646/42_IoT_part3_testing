apiVersion: v1
kind: Service
metadata:
  name: mortiz-app-service
  namespace: dev
spec:
  selector:
    app: mortiz-app
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8888
  type: ClusterIP
